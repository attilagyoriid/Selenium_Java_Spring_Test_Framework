package com.coding.challenge.selenium.framework.context;

/**
 * This enum represents the keys used in the context map.
 * Each key is associated with a specific value or data type.
 * Helps to pass values between steps
 */
public enum ContextKeys {
    RESPONSE, REQUEST, URL, STRING_VALUE, FAHRENHEIT, CELSIUS;
}
